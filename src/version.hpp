#pragma once

/*
 * Changelog
 * 1.0.0 - call45aggr
 */

 static char const* call45aggr_version = "1.0.0";

 char const* build_version();
 char const* build_datetime();
 char const* commit_hash();
 char const* commit_datetime();
 char const* commit_author_name();
 char const* commiter_name();
 char const* commit_message();
